A simple Loadable Kernel Module. The bash file 'make' the kernel module from a simple C file. It has two output texts from the initial kernel load and unload.
The bash file also cleans the folder of the created files during the 'make' process.

Run:
- "sudo bash run.sh"
